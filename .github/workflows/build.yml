name: Build EXE
on:
  push:
    branches: [master]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install pyinstaller
      - run: pyinstaller --onefile --console --name add_prefix add_prefix.py
      - uses: actions/upload-artifact@v4
        with:
          name: executables
          path: dist/*.exe
